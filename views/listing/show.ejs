<% layout("/layout/boilerplate") %>

<div class="row">
  <div class="col-8 offset-3">
    <h3 class="mt-3 text-danger"><%= list.title %></h3>
  </div>

  <div class="card col-6 offset-3">
    <img
      src="<%= list.image %>"
      class="card-img-top show_img"
      alt="listing_image"
    />
    <div class="card-body mt-2">
      <br />
      <p class="desc"><%= list.description %></p>
      <b>&#8377;<%= list.price.toLocaleString('en-IN') %> </b> <br />
      <%= list.location %> <br />
      <%= list.country %>
    </div>
  </div>
</div>

<div class="d-flex p-2 col-1 offset-3">
  <a href="/listings/<%= list._id %>/edit" class="btn btn-md btn-danger"
    >Edit</a
  >

  <form action="/listings/<%= list._id %>?_method=DELETE " method="post">
    <button type="submit" class="btn btn-md btn-danger offset-3">Delete</button>
  </form>
</div>
